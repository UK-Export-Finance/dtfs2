/*
Placeholder api test until real tests added.
Pipeline fails with no tests
TODO REMOVE when real api-tests added
*/
describe('Temporary placeholder API test until real tests added', () => {
  it('just return true', () => {
    expect(true).toEqual(true);
  });
});
