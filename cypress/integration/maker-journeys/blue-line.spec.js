context('Blue Line eligibility checking', () => {

  xit('A deal that fails blue-line checks requires additional documentation.', () => {
    //+ log in as maker
    //+ create a deal
    //+ enter data that will pass red-line checking
    //+ submit data that is going to pass red-line checking
    // enter data that will fail blue-line checking
    // submit data that is going to fail blue-line checking
    // prove the user is being prompted to upload additional documentation
  });

  xit('A deal that passes blue-line checks can progress to enter bonds/loans.', () => {
    //+ log in as maker
    //+ create a deal
    //+ enter data that will pass red-line checking
    //+ submit data that is going to pass red-line checking
    // enter data that will pass blue-line checking
    // submit data that is going to pass blue-line checking
    // prove the user is being prompted to select bonds/loans
  });

})
