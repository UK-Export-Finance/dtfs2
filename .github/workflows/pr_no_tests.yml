name: PR tests - Empty tests for github action config

on:
  pull_request:
    branches: [ main ]
    paths: 
    - '.github/workflows/**'
    - 'README.md'

jobs:
  test:
    name: Run PR tests
    runs-on: [self-hosted, linux]
    steps:
    - uses: actions/checkout@v2