import { cancelButton } from '../../../e2e/partials';

/**
 * clickCancelButton
 * Click the cancel button.
 */
const clickCancelButton = () => {
  cancelButton().click();
};

export default clickCancelButton;
