import { saveAndReturnButton } from '../../../e2e/partials';

/**
 * clickSaveAndReturnButton
 * Click the save and return button.
 */
const clickSaveAndReturnButton = () => {
  saveAndReturnButton().click();
};

export default clickSaveAndReturnButton;
