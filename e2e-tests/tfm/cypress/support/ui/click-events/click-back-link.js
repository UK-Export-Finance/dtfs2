import { clickEvents } from '../../../e2e/partials';

/**
 * clickBackLink
 * Click the back link.
 */
const clickBackLink = () => {
  clickEvents.backLink().click();
};

export default clickBackLink;
