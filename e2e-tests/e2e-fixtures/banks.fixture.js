const MOCK_BANKS = require('../../utils/mock-data-loader/banks');

export const UKEF_BANK_1 = MOCK_BANKS.find((bank) => bank.name === 'Bank 1');
export const UKEF_BANK_2 = MOCK_BANKS.find((bank) => bank.name === 'Bank 2');
