version: '3'
services:
  portal:
    container_name: portal
    build: .
    image: dtfs2/portal
    restart: always
    depends_on:
      - deal
    ports:
      - "5000:5000"

  deal:
    container_name: deal
    build: ./deal
    image: dtfs/deal
    restart: always
    ports:
      - "5001:5000"

networks:
   default:
