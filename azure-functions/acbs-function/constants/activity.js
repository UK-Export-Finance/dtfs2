const TYPE = {
  ACTIVITY: 'ACTIVITY',
};

const ROLE = {
  CHECKER: 'checker',
};

const ACCEPTED_ENDPOINTS = [
  'deal',
  'facility',
];

module.exports = {
  TYPE,
  ROLE,
  ACCEPTED_ENDPOINTS,
};
