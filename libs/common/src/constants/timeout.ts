/**
 * A collection of timeout durations used across the application.
 *
 * @property MEDIUM - Represents a medium timeout duration in milliseconds (30,000 ms or 30 seconds).
 *
 * @property LONG - Represents a long timeout duration in milliseconds (60,000 ms or 60 seconds).
 */
export const TIMEOUT = {
  MEDIUM: 30000,
  LONG: 60000,
};
