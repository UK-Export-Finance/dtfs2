// Durable orchestrated functions

/**
 * The interval between retry attempts in milliseconds.
 * @constant {number}
 */
export const RETRY_INTERVAL_MS = 5000;

/**
 * The maximum number of retry attempts.
 * @constant {number}
 */
export const MAX_RETRY = 5;
