/**
 * This type is not well defined in external-api,
 * but causes type errors in external-api where
 * `eStore` related files use the db client
 */

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type CronJobLog = any;
