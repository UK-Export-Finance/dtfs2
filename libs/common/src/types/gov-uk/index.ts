/**
 * These types allow complex Nunjucks props to be generated in typescript.
 *
 * It is generally preferable to use Nunjucks and pass in values directly instead of using JS to generate
 * specific component props, however in certain cases (e.g. complicated object to array mappings) this is not possible.
 */
export * from './summary-list';
