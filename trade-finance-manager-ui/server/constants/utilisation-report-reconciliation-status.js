/**
 * @typedef {import('../types/utilisation-reports').UtilisationReportReconciliationStatus} StatusCode
 */

/**
 * @type {Record<StatusCode, StatusCode>}
 */
const UTILISATION_REPORT_RECONCILIATION_STATUS = {
  REPORT_NOT_RECEIVED: 'REPORT_NOT_RECEIVED',
  PENDING_RECONCILIATION: 'PENDING_RECONCILIATION',
  RECONCILIATION_IN_PROGRESS: 'RECONCILIATION_IN_PROGRESS',
  RECONCILIATION_COMPLETED: 'RECONCILIATION_COMPLETED',
};

module.exports = UTILISATION_REPORT_RECONCILIATION_STATUS;
