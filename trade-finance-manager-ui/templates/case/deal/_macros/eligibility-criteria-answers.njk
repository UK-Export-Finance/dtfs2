{% import './eligibility-criteria-answer-tag.njk' as eligibilityCriteriaAnswerTag %}

{% macro render(params) %}
  {% set criteria = params.eligibilityCriteria %}

  <table class="govuk-table" data-cy="eligibility-criteria-answers">
    <tbody class="govuk-table__body">
      {% for criterion in criteria %}
        <tr class="govuk-table__row">
          <th class="govuk-table__header govuk-!-font-weight-regular govuk-!-padding-right-6" data-cy="criterion-{{criterion.id}}-heading">EC{{ criterion.id }}</th>

          <td class="govuk-table__cell">
            {{eligibilityCriteriaAnswerTag.render({ answer: criterion.answer })}}
          </td>

          <td class="govuk-table__cell govuk-!-padding-left-8">
            <div class="govuk-grid-column-three-quarters govuk-!-padding-0">
              <p class="govuk-body govuk-!-margin-bottom-2" data-cy="criterion-{{criterion.id}}-description">{{ criterion.description }}</p>

              {% if criterion.descriptionList.length > 0 %}
                <ul class="govuk-list govuk-list--bullet govuk-!-margin-bottom-2">
                  {% for item in criterion.descriptionList %}
                    <li data-cy="criterion-{{criterion.id}}-descriptionList-{{ loop.index }}">{{ item }}</li>
                  {% endfor %}
                </ul>
              {% endif %}
            </div>
          </td>

        </tr>
      {% endfor %}
    </tbody>
  </table>


{% endmacro %}
