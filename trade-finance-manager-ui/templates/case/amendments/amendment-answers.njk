{% from "govuk/components/table/macro.njk" import govukTable %}
{% from "govuk/components/button/macro.njk" import govukButton %}
{% from "govuk/components/summary-list/macro.njk" import govukSummaryList %}

{% extends "index.njk" %}

{% block pageTitle %}
  Amendment - Check answers
{% endblock %}

{% block sub_content %}
  {% if errors %}
    {{ govukErrorSummary({
      titleText: "There is a problem",
      errorList: errors.errorSummary,
      attributes: {
        'data-cy': 'amendment--error-summary'
      },
      classes: "govuk-!-margin-top-4 govuk-!-margin-bottom-4"
    }) }}
  {% endif %}

  {% set optionsWithHTML = [] %}

  {% set bankRequestDateUrl %}
      <a
        class="govuk-link"
        href="/case/{{ dealId }}/facility/{{ facilityId }}/amendment/{{ amendmentId }}/request-date"
        aria-label="View bank request date">
        Change
      </a>
  {% endset %}
  {% set defaultBankRequestDate =
    {
      key: { text: 'Bank requested change' },
      value: { text: requestDate, attributes: { "data-cy": "amendment--bank-request-date-response" } },
      actions: { items: [ { html: bankRequestDateUrl, attributes: { "data-cy": "amendment--bank-request-date-link" } } ] }
    }
  %}
  {{- optionsWithHTML.push(defaultBankRequestDate) | reject() }}

  {% set requireUkefApprovalUrl %}
      <a
        class="govuk-link"
        href="/case/{{ dealId }}/facility/{{ facilityId }}/amendment/{{ amendmentId }}/request-approval"
        aria-label="View UKEF approval">
        Change
      </a>
  {% endset %}
  {% set defaultRequireUkefApproval =
    {
      key: { text: 'UKEF approval needed' },
      value: { text: "Yes" if requireUkefApproval === true else "No", attributes: { "data-cy": "amendment--require-approval-response" } },
      actions: { items: [ { html: requireUkefApprovalUrl, attributes: { "data-cy": "amendment--require-approval-link" } } ] }
    }
  %}
  {{- optionsWithHTML.push(defaultRequireUkefApproval) | reject() }}

  {% if requireUkefApproval === false %}
    {% set effectiveDateUrl %}
        <a
          class="govuk-link"
          href="/case/{{ dealId }}/facility/{{ facilityId }}/amendment/{{ amendmentId }}/amendment-effective-date"
          aria-label="View effective date">
          Change
        </a>
    {% endset %}
    {% set defaultEffectiveDate =
      {
        key: { text: 'Date the amendment will be effective from' },
        value: { text:  effectiveDate, attributes: { "data-cy": "amendment--effective-date-response" } },
        actions: { items: [ { html: effectiveDateUrl, attributes: { "data-cy": "amendment--effective-date-link" } } ] }
      }
    %}
    {{- optionsWithHTML.push(defaultEffectiveDate) | reject() }}
  {% endif %}

  {% if changeCoverEndDate === true %}
    {% set coverEndDateUrl %}
        <a
          class="govuk-link"
          href="/case/{{ dealId }}/facility/{{ facilityId }}/amendment/{{ amendmentId }}/cover-end-date"
          aria-label="Change facility value">
          Change
        </a>
    {% endset %}
    {% set defaultCoverEndDate =
      {
        key:{ text: 'New cover date' },
        value: { text: coverEndDate, attributes: { "data-cy": "amendment--cover-end-date-response" } },
        actions: { items: [ { html: coverEndDateUrl, attributes: { "data-cy": "amendment--cover-end-date-link" } } ] }
      }
    %}
    {{- optionsWithHTML.push(defaultCoverEndDate) | reject() }}
  {% endif %}

  {% if changeFacilityValue === true %}
    {% set facilityValueUrl %}
        <a
          class="govuk-link"
          href="/case/{{ dealId }}/facility/{{ facilityId }}/amendment/{{ amendmentId }}/facility-value"
          aria-label="Change facility value">
          Change
        </a>
    {% endset %}
    {% set defaultFacilityValue =
      {
        key: { text: 'New facility value' },
        value: { text: value, attributes: { "data-cy": "amendment--facility-value-response" } },
        actions: { items: [ { html: facilityValueUrl, attributes: { "data-cy": "amendment--facility-value-link" } } ] }
      }
    %}
    {{- optionsWithHTML.push(defaultFacilityValue) | reject() }}
  {% endif %}

  <div class="govuk-grid-row">
    <div class="govuk-grid-column-one-half">
      <h1 class="govuk-heading-l govuk-!-margin-top-4 govuk-!-margin-bottom-4" data-cy="amendment--answers-heading" aria-label="Check your answers">Check your answers</h1>

      <form method="POST" autocomplete="off">
        <input type="hidden" name="_csrf" value="{{ csrfToken }}">

        {{ govukSummaryList({
          rows: optionsWithHTML
        }) }}



        <div class="govuk-button-group govuk-!-margin-top-4">
          {% if isEditable %}
            {{ govukButton({
              text: "Submit",
              attributes: { "data-cy": "amendment--continue-button" }
              })
            }}
          {% endif %}
          <a class="govuk-link" href="/case/{{ dealId }}/facility/{{ facilityId }}#amendments" data-cy="amendment--cancel-button">Cancel</a>
        </div>
      </form>
    </div>
  </div>
{% endblock %}
