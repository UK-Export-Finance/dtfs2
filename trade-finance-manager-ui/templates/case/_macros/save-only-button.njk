# This macro renders primary `Save` button only

{% from "govuk/components/button/macro.njk" import govukButton %}

{% macro render(params) %}
  
  <div class="govuk-!-margin-top-4">
    {{ govukButton({
      text: "Save and close",
      attributes: {
        'data-cy': 'save-button'
      }
    }) }}
  </div>

{% endmacro %}
