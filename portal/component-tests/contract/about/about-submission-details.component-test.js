/*
const pageRenderer = require('../../pageRenderer');
const deal = require('../../fixtures/deal-fully-completed');

const page = 'contract/about/components/about-submission-details.njk';

const render = pageRenderer(page);

describe(page, () => {
  let wrapper;

  beforeEach(() => {
    wrapper = render({ deal });
  });

  it('should...', () => {
    
  });
});
*/
