const { bssDeals, gefDeals, allDeals } = require('./deals');
const { bssFacilities, gefFacilities } = require('./facilities');

module.exports = {
  allDeals,
  bssDeals,
  gefDeals,
  bssFacilities,
  gefFacilities,
};
