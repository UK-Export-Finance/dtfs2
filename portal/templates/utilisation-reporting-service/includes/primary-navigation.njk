{%- from "moj/components/primary-navigation/macro.njk" import mojPrimaryNavigation -%}

{% if user and user.roles.includes("admin")%}

{{ mojPrimaryNavigation({
  label: "Primary navigation",
  items: [
  {
    text: "Upload report",
    href: "/gef/utilisation-report-upload",
    active: primaryNav == "upload_report",
    attributes: {
      "data-cy": "upload_report"
    }
  },
  {
    text: "Reports",
    href: "/reports",
    active: primaryNav == "reports",
    attributes: {
      "data-cy": "reports"
    }
  },
  {
    text: "Users",
    href: "/admin/users",
    active: primaryNav == "users",
    attributes: {
      "data-cy": "users"
    }
  }
  ]
}) }}

{% elif user %}

{{ mojPrimaryNavigation({
  label: "Primary navigation",
  items: [
  {
    text: "Upload report",
    href: "/gef/utilisation-report-upload",
    active: primaryNav == "upload_report",
    attributes: {
      "data-cy": "upload_report"
    }
  },
  {
    text: "Previous reports",
    href: "/gef/previous-reports",
    active: primaryNav == "previous_reports",
    attributes: {
      "data-cy": "previous_reports"
    }
  }
  ]
}) }}

{% endif %}
